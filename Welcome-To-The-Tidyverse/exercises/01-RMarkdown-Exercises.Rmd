---
title: "R Markdown"
---

```{r setup}
library(tidyverse)
```


Este es un archivo R Markdown. Contiene texto plano intercalado con chunks de c??digo. Pod??s usar este archivo para tomar notas _y_ ejecutar c??digo. Por ejemplo, pod??s escribir tu nombre en la l??nea siguiente. Intentalo:

Rodrigo

```{r}
# Pod??s escribir c??digo en chunks como este.
# Este chunk usa el c??digo plot(cars) para graficar un dataset.
# Para ejecutar el c??digo, hac?? click en el bot??n verde de Play 
# en la esquina superior derecha de este chunk. Probalo!
plot(cars)
```

Bien hecho! Los resultado del chunk de c??digo aparecen abajo del chunk. Pod??s hacer click en la x encima de los resultados para borrarlos, pero no hagamos eso ahora.

Pod??s abrir un nuevo archivo R Markdown yendo a **File > New File > R Markdown...**. Luego hac?? click en OK. Pero sigamos leyendo este archivo por ahora!

# A??adiendo chunks

Para a??adir un nuevo chunk de c??digo pod??s presionar *Cmd+Option+I* (*Ctrl+Alt+I* en Windows), o hacer click al bot??n *Insert* en la parte superior de este documento y luego seleccionar *R*. R Markdown a??adir?? un chunk nuevo y vac??o en la posici??n actual del cursor.

Trat?? de a??adir un chunk de c??digo abajo:

```{r}

```


??Buen trabajo! Por hoy, todo tu c??digo de R debe ir adentro de chunks.

```{r}
# A veces podr??as querer ejecutar solo parte del c??digo 
# en un chunk. Para hacerlo, seleccion?? el c??digo
# a ejecutar y presion?? Cmd + Enter (Control + Enter en 
# Windows). Si no seleccion??s ning??n c??digo R va a 
# ejecutar la l??nea de c??digo donde est?? tu cursor.
# Probalo ahora. Ejecut?? mean(1:5) pero no la l??nea siguiente.
mean(1:5)
warning("You shouldn't run this!")
```

```{r}
# Pod??s hacer click en la flecha que apunta para abajo para ejecutar 
# todos los chunks arriba del actual.Esto es ??til si el c??digo en tu
# chunk usa objeto que construiste en chunks anteriores.
# Sys.Date()
```

??Notaste las l??neas m??s claras en el chunk anterior? Son *comentarios*, l??neas de texto que R ignora al ejecutar el c??digo. R trata todo lo que aparece despu??s de un `#` en una l??nea como un comentario. Como resultado, si ejecut??s el chunk anterior, no pasa nada (y eso est?? bien).

Borr?? el `#` en la ??ltima l??nea del chunk anterior y ejecutalo de vuelta. ??Pod??s decirme que hace `Sys.Date()`?

Por cierto, solo necesit??s usar comentario _dentro_ de los chunks de c??digo. R sabe que no debe ejecutar el texto que escribas fuera de los chunks de c??digo.

# Formateo de texto

??Te dist cuenta del resaltado particular en este documento? R Markdown trata el texto rodeado
de *asteriscos*, **asteriscos dobles**, y `backticks` de manera especial. Es la forma particularIt de R Markdown de decir que estas palabras est??n en

- _cursiva_
- *tambi??n cursiva*
- **negrita**, and
- `formato de c??digo`

`*`, `**`, y \` son se??ales usadas por un formato de edici??n de texto llamado `markdown`. R Markdown usa `markdown` para convertir tus archivos planos .Rmd en reportes pulidos. Prob??moslo. 

# Reportes

Al hacer click en el bot??n `knit` en la parte superior de un archivo R Markdown (como este), R Markdown una versi??n pulida de tu reporte. R Markdown:

1. Transforma toda las se??ales markdown en texto formateado (por ejemplo texto en negritas, texto en cursivas, etc.)
2. Ejecuta nuevamente todos los chunks de c??digo en una sesi??n nueva de R y a??ade los resultados al reporte.
3. Guarda el reporte finalizado junto a tu archivo .Rmd

Hac?? click en el bot??n *knit* en la parte superior de este documento o presion?? *Cmd+Shift+K* (*Ctrl+Shift+K* on Windows) para renderizar el reporte final. El IDE RStudio abrir?? el reporte para que puedas ver su contenido. Por ahora, nuestros reportes ser??n archivos HTML. Prob?? haciendo click en *Knit* ahora.

??Buen trabajo! Vas a aprender m??s sobre R Markdown en estos d??as.

# Paquetes de R

Aqu?? hay un ??ltimo chunk de c??digo que usaremos en el pr??ximo ejericios. Si descoment??s el c??digo y trat??s de ejecutarlo, no va a funcionar. Si no me cre??s, ??intentalo!


```{r}
ggplot(data = diamonds) + geom_point(aes(x = carat, y = price))
```
